require("pon")

TLG.SERV = SERVERS:Name()

function TLG.DBQ(sQuery,fCallback)
	QS(sQuery,fCallback)
end

table.ConcatKeys = table.ConcatKeys
NM = NM

-- SERVERS:ID(), SERVER:Name()
-- includeSV()
-- bib.set bib.get (Simple Redis Analogue)

-- LoadModules()

-- math.digits(i) -- digits count in number. 123 = 3. Used in keypad
